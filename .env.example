# Restaurant list data source.
#
# In Google Sheets: File → Share → Publish to web → choose the sheet tab →
# "Comma-separated values (.csv)" → copy that URL here.
#
# Set this in Vercel too: Project → Settings → Environment Variables.
# While unset, the site shows built-in sample data so the page still works.
RESTAURANTS_CSV_URL=
